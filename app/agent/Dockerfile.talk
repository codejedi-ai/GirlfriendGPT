FROM python:3.11-slim

WORKDIR /app

RUN pip install --no-cache-dir uv

COPY pyproject.toml ./
RUN uv sync --no-dev

COPY talk ./talk
COPY .env.example ./

ENV PYTHONPATH=/app
ENV TALK_HOST=0.0.0.0
ENV TALK_PORT=8080

EXPOSE 8080

CMD ["uv", "run", "python", "-m", "talk.server"]
